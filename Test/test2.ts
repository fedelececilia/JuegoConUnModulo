//var gr = require("../js/getRandomChoice.ts");
import {getRandomChoice} from '../js/getRandomChoice'
import assert from "assert"

describe('getRC', () => {
    it('should return', () => {
        const result = getRandomChoice();
        assert.equal(1, 1);
    })
})